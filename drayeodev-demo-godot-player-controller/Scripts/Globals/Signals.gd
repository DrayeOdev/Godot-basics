extends Node


## NOTE: These signals are MARKED as unused because this script NEVER uses them however that is NOT
## the case, these signals are used in other scripts. I just dont like having the line highlighted XD

@warning_ignore_start("unused_signal")
signal PlayerMovementDirection(Vector3)
